namespace BowThrusterChallenge.Settings
{
    public class Settings
    {
        public WindowSettings Window { get; set; }   // Window settings (width, height)
        public ControlSettings Controls { get; set; } // Control settings (keys for forward, left, right)
    }
}
